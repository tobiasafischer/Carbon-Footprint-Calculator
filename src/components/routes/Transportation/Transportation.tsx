import React from 'react'
import styled from 'styled-components'

const Container = styled.div``

const Trasportation: React.FC = ({ children }) => (
   // lol
   <Container>{children}</Container>
)

export default Trasportation
